# User guide

## Article Hub

1. 執行 `python3 scripts/build_article_hub.py`，再以 `python3 -m http.server 8123` 啟動本機預覽。
2. 開啟 `http://127.0.0.1:8123/index.html`，從文章主題卡片進入學習頁。
3. 新增文章時，複製 `content/articles/article_01/` 的格式，更新 `article.json` 與 `article.md` 後重新建置。
4. 快問、自我整理、案例、工作紀錄與行動承諾會保存於目前瀏覽器；清除網站資料會刪除紀錄。

目前已有兩篇示範文章；第二篇聚焦 EQ 從個人覺察、領導影響到團隊氣氛的三層練習。

文章頁的自我整理是發展工具，不是心理測驗或人資決策工具；案例請移除姓名、客戶名稱與其他可辨識資訊。

Unknown / needs confirmation.
# 使用指南

1. 在 `/Users/wes_mini/Projects/Leadership Principle hub` 啟動本機伺服器，開啟 `index.html`，從 Module 01–06 選擇主題。
2. 每個模組依序完成深入閱讀、工作案例、發展自評、工作紀錄與行動承諾。
3. 自評答案與文字欄位會自動儲存在目前瀏覽器；若要保留結果，可使用瀏覽器列印成 PDF。
4. 建議先完成 Module 01，再依一週行動結果選擇下一個模組；不要把分數當成能力排名。
5. 管理者請在專案目錄執行 `python3 admin_server.py`，再開啟 `http://127.0.0.1:8765/admin.html`。輸入設定密碼後選擇章節，可在右側預覽直接修改文字、調整閱讀字體大小與文字顏色，按「儲存並更新網站」後直接寫回 HTML。

6. 每章深入閱讀會提供約三個概念說明、專有名詞解釋或工具箱專欄，並在最後以「總結提要」整理可帶走的重點。

7. 每章開始時可先在「開始前，先想一想」留下直覺判斷；深入閱讀前有四點定位及三題折疊式情境快問。快問會保存選擇與回饋；「清除本章答案」會清除快問與自評答案，但不會刪除開始提示或工作紀錄。

8. 發展自評會顯示「已完成 X／12」。若仍有缺答，按下結果按鈕後頁面會移到第一題缺答處，並在題目前及按鈕附近顯示提醒。四個構面相近時不會指定建議答案，請依頁面上的工作情境問題自行選擇焦點。

9. 在案例與工作紀錄中，請移除姓名、客戶名稱及其他可辨識資訊。

10. 第三章皇家知識沙龍版是視覺實驗，位於 `experiments/chapter03-royal/LPI_Coach_Chapter03_Royal.html`，不屬於首頁正式章節入口。它使用獨立的本機儲存空間，不會覆蓋原版第三章紀錄。

11. 首頁及每章頁尾均可開啟林祖威教練介紹；點擊電子郵件地址會在新分頁開啟 Gmail 撰寫畫面，並自動帶入 `wesley.lin@leading4elite.com` 收件人。

管理編輯只綁定 `127.0.0.1`，不接受區域網路或外部連線。它會直接改寫本目錄的章節 HTML；建議修改前先備份。密碼仍是本機管理用途，不是多人網站的完整身分驗證。
